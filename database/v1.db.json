{
  "up": [
    {
      "description": "Creating M_CATEGORY table",
      "statement": "CREATE TABLE IF NOT EXISTS `m_category` (`id` char(36) NOT NULL,`category_name` varchar(255) DEFAULT NULL,`created_at` datetime DEFAULT CURRENT_TIMESTAMP,`updated_at` datetime DEFAULT CURRENT_TIMESTAMP,`deleted_at` datetime DEFAULT NULL,PRIMARY KEY (`id`))"
    },
    {
      "description": "Creating M_PRODUCT table",
      "statement": "CREATE TABLE IF NOT EXISTS `m_product` (`id` char(36) NOT NULL,`product_code` varchar(255) NOT NULL,`product_name` varchar(255) NOT NULL,`created_at` datetime DEFAULT CURRENT_TIMESTAMP,`updated_at` datetime DEFAULT CURRENT_TIMESTAMP,`deleted_at` datetime DEFAULT NULL,`category_id` char(36) DEFAULT NULL,PRIMARY KEY (`id`),UNIQUE KEY `product_code_UNIQUE` (`product_code`),KEY `category_id` (`category_id`),CONSTRAINT `m_product_ibfk_1` FOREIGN KEY (`category_id`) REFERENCES `m_category` (`id`) ON DELETE SET NULL ON UPDATE CASCADE)"
    },
    {
      "description": "Creating M_PRODUCT_PRICE table",
      "statement": "CREATE TABLE IF NOT EXISTS `m_product_price` (`product_price_id` char(36) NOT NULL,`product_id` char(36) NOT NULL,`product_price` int NOT NULL DEFAULT '0',`is_active` varchar(1) DEFAULT '0',PRIMARY KEY (`product_price_id`))"
    }
  ],
  "down": [
    {
      "description": "Dropping M_PRODUCT table",
      "statement": "DROP TABLE `m_product`"
    },
    {
      "description": "Dropping M_PRODUCT_PRICE table",
      "statement": "DROP TABLE `m_product_price`"
    },{
      "description": "Dropping M_CATEGORY table",
      "statement": "DROP TABLE `m_category`"
    }
  ]
}